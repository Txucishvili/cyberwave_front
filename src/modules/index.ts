
// import {partition} from 'lodash';

// console.log('a', partition([1, 2, 3, 4], n => n % 2));
// console.log('b', defaults({ 'a': 1 }, { 'a': 3, 'b': 2 }));

const Modules: any = [];
console.log('[Modules] index 2 hot change 2')

export default Modules;
